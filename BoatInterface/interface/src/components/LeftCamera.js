import { Box } from "@mui/material";
import React from "react";

const LeftCamera = () => {
  return (
    <Box sx={{ height: 500, backgroundColor: "blue" }}>
      <Box sx={{ color: "white", mx: "auto", textAlign: "center" }}>
        Left Camera
      </Box>
    </Box>
  );
};

export default LeftCamera;
